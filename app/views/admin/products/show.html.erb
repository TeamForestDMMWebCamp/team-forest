<div class="container">
	<div class="row">

		<h2>商品詳細</h2>

		<div class="col-lg-3">
						<%= attachment_image_tag(@product, :image, fallback: "no_image.jpg", size: "250x250") %>
		</div>
		<div class="col-lg-9">
			<table class="table table-hover">
				<tbody>
					<tr>
						<th class="table-noborder">商品名</th>
						<td class="table-noborder"><%= @product.name %></td>

					</tr>
					<tr>
						<th class="table-noborder">商品説明</th>
						<td class="table-noborder"><%= @product.discription %></td>
					</tr>
					<tr>
						<th class="table-noborder">ジャンル</th>
						<td class="table-noborder"><%= @product.genre.name %></td>
					</tr>
					<tr>
						<th nowrap class="table-noborder">税込価格(税抜価格)</th>
						<td class="table-noborder"><%= @product.including_tax %>円(<%= @product.price_excluding_tax %>)円</td>
					</tr>
					<tr>
						<th nowrap class="table-noborder">販売ステータス</th>
						<td class="table-noborder">
							<% if @product.is_active == "Availble"  %>
					        <%= "販売中" %>
					        <% else %>
					        <%= "売り切れ" %>
					        <% end %>
	     			</td>
					</tr>
					<tr>
						<th class="table-noborder"> </th>
						<td align="right" class="table-noborder">
							<%= link_to "編集ページへ", edit_admin_product_path, class:"btn btn-info btn-md" %>
						</td>
					</tr>
				</tbody>
			</table>
		</div>
	</div>
</div>